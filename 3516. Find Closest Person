class Solution {
public:
    static int findClosest(int x, int y, int z) {

        int xDist = abs(x - z);
        int yDist = abs(y - z);

        if (xDist == yDist) return 0;
        else {
            if (xDist > yDist) return 2;
            else return 1;
        }
    }
};
