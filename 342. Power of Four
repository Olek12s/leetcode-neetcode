class Solution {
public:
    static bool isPowerOfFour(int n) {
        if (n <= 0) return false;
        unsigned int mask = 1;

        for (int i = 0; i < 16; i++) {
            if ((mask ^ n) == 0) return true;
            mask <<= 2;
        }
        return false;
    }
};
